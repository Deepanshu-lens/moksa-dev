---
import Layout from "@/layouts/Layout.astro";
import PlaybackLayout from "@/components/playback/layout.svelte";
import Camera from "@/managers/camera.svelte";
import Node from "@/managers/node.svelte";
---

<Layout title={import.meta.env.PUBLIC_BRAND_NAME + " | Playback"} topbar> 
  <Node client:only="svelte" />
  <Camera client:only="svelte" />
  <PlaybackLayout client:only="svelte" />
</Layout>
