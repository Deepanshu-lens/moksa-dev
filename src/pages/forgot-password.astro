---
import AuthShell from "@/layouts/AuthShell.astro";
import ForgotPasswordLayout from "@/components/forgot-password/layout.svelte";
export const prerender = true;
---

<AuthShell
  title={import.meta.env.PUBLIC_BRAND_NAME + " | Forgot Password"}
  mode="forgot-password"
>
  <ForgotPasswordLayout client:only="svelte" />
</AuthShell>
